# Optional: enables real AI responses from Tora via the Anthropic API.
# Get a key at https://console.anthropic.com — without it, Tora runs in
# a helpful rule-based offline mode instead of calling the API.
ANTHROPIC_API_KEY=
